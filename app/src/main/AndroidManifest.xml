<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.inruca.smartbag">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bag_vector"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bag_vector"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/AppTheme.SplashScreen"
            android:noHistory="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ScannerActivity"
            android:icon="@drawable/ic_blinky_feature"
            android:label="@string/feature_name"
        >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="no.nordicsemi.android.nrftoolbox.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".SmartBagActivty"
            android:launchMode="singleTop"
            android:parentActivityName=".ScannerActivity">
        </activity>
        <activity android:name=".SmartBagDashboard"
            android:launchMode="singleTop">
        </activity>

    </application>

    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="no.nordicsemi.android.LOG"/>

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true"/>



</manifest>